Git repository
snapshots of repository
git status											// Lists changes that haven't been updated
git fetch
git add/rm <file/dir>								// used to add or remove file/dir from database
// ex: git add . -A									// adds everything in the current working dir. Usefull if changes listed in 'git status'
git commit
git push											// Changes local data
git rebase <branch_name>
git checkout <branch_name> or git checkout <file/dir>
git pull
git reset (usually use as 'git reset –hard <branch_name>')
git merge <branch_name>

git clone https://github.com/zeke225/cs360			// Grabs code database from my cs360

git log --pretty=format:"%an, %ar : %s"				// Shows history of edits to log (those messages you write when you 'git commit')